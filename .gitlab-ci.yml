image: haskell:8

services:
  - docker:dind

before_script:
  - cabal update
  - cabal install  random optparse-applicative libmpd

build:
  stage: build
  script:
    # - runhaskell Setup.hs configure
    # - runhaskell Setup.hs build
    - make
